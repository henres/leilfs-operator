---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: saunafsclusters.saunafs.saunafs-operator.io
spec:
  group: saunafs.saunafs-operator.io
  names:
    kind: SaunaFSCluster
    listKind: SaunaFSClusterList
    plural: saunafsclusters
    singular: saunafscluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Whether all cluster components are reconciled
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: Reason for the current Ready status
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - description: Pod currently holding the master Lease
      jsonPath: .status.activeMaster
      name: ActiveMaster
      type: string
    - description: Number of ready shadow replicas
      jsonPath: .status.readyShadows
      name: Shadows
      type: integer
    - description: Ready chunk server count
      jsonPath: .status.readyChunkServers
      name: ChunkServers
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SaunaFSCluster is the Schema for the saunafsclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SaunaFSClusterSpec defines the desired state of SaunaFSCluster
            properties:
              chunk:
                description: Chunk holds defaults shared by all chunk servers and
                  the per-server list.
                properties:
                  image:
                    description: Image is the default container image used for all
                      chunk servers.
                    type: string
                  ports:
                    description: Ports exposed by each chunk server container.
                    items:
                      description: NamedPort is a simple named container port.
                      properties:
                        containerPort:
                          format: int32
                          type: integer
                        name:
                          type: string
                      type: object
                    type: array
                  servers:
                    description: |-
                      Servers is the list of individually configured chunk servers.
                      Each entry results in a dedicated Pod (or StatefulSet) on the target node.
                    items:
                      description: ChunkServerSpec describes a single chunk server
                        instance.
                      properties:
                        image:
                          description: Image overrides the default chunk image for
                            this server only.
                          type: string
                        label:
                          description: |-
                            Label is the LABEL value written into sfschunkserver.cfg.
                            It is used by SaunaFS storage goals to pin chunks to specific servers
                            (e.g. one label per physical node ensures node-level data spreading).
                            When empty, no LABEL line is written and the chunkserver is anonymous.
                          type: string
                        mountPaths:
                          description: MountPaths lists the host paths or PVC mount
                            points used as chunk storage.
                          items:
                            description: MountPath describes a single storage mount
                              for a chunk server.
                            properties:
                              claimName:
                                description: ClaimName references an existing PersistentVolumeClaim
                                  to mount.
                                type: string
                              hostPath:
                                description: HostPath uses a directory from the host
                                  node directly (for local disks).
                                type: string
                              path:
                                description: Path is the mount point inside the container
                                  (e.g. /mnt/hdd001).
                                type: string
                              size:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              storageClassName:
                                description: |-
                                  StorageClassName and Size are used to dynamically provision a PVC if
                                  neither HostPath nor ClaimName is set.
                                type: string
                            required:
                            - path
                            type: object
                          type: array
                        name:
                          description: Name is a unique identifier for this chunk
                            server (used as pod/sts suffix).
                          type: string
                        nodeName:
                          description: NodeName directly schedules this chunk server
                            onto a named node.
                          type: string
                        resources:
                          description: Resources overrides the default resource requirements
                            for this server.
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.


                                This is an alpha field and requires enabling the
                                DynamicResourceAllocation feature gate.


                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        tolerations:
                          description: Tolerations for this chunk server instance.
                          items:
                            description: |-
                              The pod this Toleration is attached to tolerates any taint that matches
                              the triple <key,value,effect> using the matching operator <operator>.
                            properties:
                              effect:
                                description: |-
                                  Effect indicates the taint effect to match. Empty means match all taint effects.
                                  When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                                type: string
                              key:
                                description: |-
                                  Key is the taint key that the toleration applies to. Empty means match all taint keys.
                                  If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                                type: string
                              operator:
                                description: |-
                                  Operator represents a key's relationship to the value.
                                  Valid operators are Exists and Equal. Defaults to Equal.
                                  Exists is equivalent to wildcard for value, so that a pod can
                                  tolerate all taints of a particular category.
                                type: string
                              tolerationSeconds:
                                description: |-
                                  TolerationSeconds represents the period of time the toleration (which must be
                                  of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                                  it is not set, which means tolerate the taint forever (do not evict). Zero and
                                  negative values will be treated as 0 (evict immediately) by the system.
                                format: int64
                                type: integer
                              value:
                                description: |-
                                  Value is the taint value the toleration matches to.
                                  If the operator is Exists, the value should be empty, otherwise just a regular string.
                                type: string
                            type: object
                          type: array
                      required:
                      - name
                      - nodeName
                      type: object
                    minItems: 1
                    type: array
                required:
                - servers
                type: object
              csi:
                description: CSI controls the optional CSI driver deployment.
                properties:
                  controllerReplicas:
                    format: int32
                    type: integer
                  driverName:
                    type: string
                  enabled:
                    type: boolean
                  image:
                    type: string
                  nodeSelector:
                    additionalProperties:
                      type: string
                    type: object
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              expose:
                description: |-
                  Expose controls optional NodePort exposure so that external SaunaFS
                  clients (saunafs-client) can mount the filesystem from outside the cluster.
                properties:
                  adminNodePort:
                    description: AdminNodePort is the optional NodePort for the admin
                      port (9419).
                    format: int32
                    maximum: 32767
                    minimum: 30000
                    type: integer
                  clientNodePort:
                    description: |-
                      ClientNodePort is the NodePort assigned to the SaunaFS client port (9421).
                      When 0 (the default) Kubernetes assigns a random port in the NodePort range.
                    format: int32
                    maximum: 32767
                    minimum: 30000
                    type: integer
                  enabled:
                    description: |-
                      Enabled controls whether a NodePort Service is created so that external
                      SaunaFS clients can connect to the master and mount the filesystem.
                    type: boolean
                type: object
              goals:
                description: |-
                  Goals defines custom storage goals written to sfsgoals.cfg on the master.
                  Each entry produces one line in the file; the master reads it at startup.
                  If omitted, SaunaFS uses its built-in defaults (goals 1–9).
                  Goals with IDs 1–9 override the built-in ones; IDs 10–20 are purely
                  custom. The first entry flagged with Default=true is applied as the
                  cluster-wide default goal via the master configuration.
                items:
                  description: "GoalSpec defines one SaunaFS storage goal written
                    to sfsgoals.cfg.\nExactly one of Replication or EC must be set.\n\n\nBuilt-in
                    SaunaFS goals use IDs 1–9; custom goals should use 10–20.\n\n\nExamples:\n\n\n\t{
                    id: 2, name: \"two_copies\",  replication: 2 }\n\t{ id: 10, name:
                    \"ec_4_2\", ec: { dataParts: 4, parityParts: 2 }, default: true
                    }\n\t{ id: 11, name: \"node_spread\", replication: 3, nodeLabels:
                    [\"node1\", \"node2\", \"node3\"] }"
                  properties:
                    default:
                      description: |-
                        Default marks this goal as the cluster-wide default.
                        At most one goal should set Default=true; the operator writes
                        SFSMASTER_DEFAULT_GOAL=<Name> in sfsmaster.cfg.
                      type: boolean
                    ec:
                      description: |-
                        EC defines an erasure-coding goal.
                        Mutually exclusive with Replication.
                      properties:
                        dataParts:
                          description: DataParts is the number of data fragments (k
                            in ec(k,m)).
                          format: int32
                          minimum: 2
                          type: integer
                        parityParts:
                          description: ParityParts is the number of parity/redundancy
                            fragments (m in ec(k,m)).
                          format: int32
                          minimum: 1
                          type: integer
                      required:
                      - dataParts
                      - parityParts
                      type: object
                    id:
                      description: |-
                        ID is the numeric identifier for this goal (1–20).
                        SaunaFS built-in goals occupy IDs 1–9.
                      format: int32
                      maximum: 20
                      minimum: 1
                      type: integer
                    name:
                      description: |-
                        Name is the human-readable label used in sfsgoals.cfg.
                        Must contain only alphanumeric characters, hyphens, or underscores.
                      pattern: ^[a-zA-Z0-9_-]+$
                      type: string
                    nodeLabels:
                      description: |-
                        NodeLabels constrains replication copies to specific chunkserver labels.
                        When set, each element corresponds to one copy and must match the LABEL
                        set in sfschunkserver.cfg on the target chunkserver. The pattern becomes
                        "{ label1 _ } { label2 _ } …" instead of anonymous underscores.
                        The number of NodeLabels entries must equal Replication when both are set.
                        Ignored when EC is set.
                      items:
                        type: string
                      type: array
                    replication:
                      description: |-
                        Replication is the number of copies to maintain.
                        Mutually exclusive with EC.
                      format: int32
                      maximum: 20
                      minimum: 1
                      type: integer
                  required:
                  - id
                  - name
                  type: object
                type: array
              interface:
                description: WebUI controls the optional CGI web interface (saunafs-cgiserver).
                properties:
                  enabled:
                    description: Enabled controls whether the CGI interface Deployment
                      is created.
                    type: boolean
                  image:
                    description: Image overrides the default saunafs-cgiserver image
                      (ghcr.io/henres/saunafs-container/saunafs-cgiserver:latest).
                    type: string
                  nodeSelector:
                    additionalProperties:
                      type: string
                    type: object
                  port:
                    description: Port is the HTTP port exposed by the cgiserver (defaults
                      to 9425).
                    format: int32
                    type: integer
                  replicas:
                    description: Replicas is the number of cgiserver pods (defaults
                      to 1).
                    format: int32
                    type: integer
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  serviceType:
                    description: 'ServiceType controls how the Service is exposed
                      (default: ClusterIP).'
                    type: string
                  tolerations:
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              master:
                description: Master configures the master daemonset.
                properties:
                  image:
                    type: string
                  metadataStorage:
                    description: |-
                      MetadataStorage configures the PVC used to persist /var/lib/saunafs.
                      If omitted, a default 1Gi PVC is created automatically.
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        default: 1Gi
                        description: |-
                          Size is the storage capacity requested for the metadata PVC.
                          Defaults to "1Gi".
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: |-
                          StorageClassName is the name of the StorageClass to use for the metadata PVC.
                          Defaults to "" (cluster default StorageClass).
                        type: string
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    type: object
                  ports:
                    items:
                      description: NamedPort is a simple named container port.
                      properties:
                        containerPort:
                          format: int32
                          type: integer
                        name:
                          type: string
                      type: object
                    type: array
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  serviceType:
                    description: Service Type string describes ingress methods for
                      a service
                    type: string
                  startupGracePeriod:
                    description: |-
                      StartupGracePeriod is how long the ha-sidecar waits after pod start
                      before it begins monitoring sfsmaster health via pgrep. This covers
                      the time needed to load metadata (proportional to filesystem size).
                      Defaults to 30s. Increase for large filesystems with many inodes.
                    type: string
                  tolerations:
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              metalogger:
                description: |-
                  Metaloggers configures optional metalogger instances that shadow the
                  master metadata (warm standby). Each metalogger connects to the master
                  and maintains an up-to-date copy of the metadata journal.
                properties:
                  image:
                    description: Image overrides the default saunafs-metalogger container
                      image.
                    type: string
                  metadataStorage:
                    description: |-
                      MetadataStorage configures the PVC template used by the metalogger
                      StatefulSet to persist its copy of the metadata journal.
                      Each replica gets its own PVC.
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        default: 1Gi
                        description: |-
                          Size is the storage capacity requested for the metadata PVC.
                          Defaults to "1Gi".
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: |-
                          StorageClassName is the name of the StorageClass to use for the metadata PVC.
                          Defaults to "" (cluster default StorageClass).
                        type: string
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector for metalogger pods.
                    type: object
                  replicas:
                    default: 0
                    description: Replicas is the number of metalogger instances (default
                      0 = disabled).
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: Resources for the metalogger container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    description: Tolerations for metalogger pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              nfs:
                description: |-
                  NFS controls an optional NFS-Ganesha gateway that re-exports the SaunaFS
                  filesystem over standard NFS (port 2049). Any NFS client can then mount
                  the filesystem without installing saunafs-client.
                properties:
                  enabled:
                    description: Enabled controls whether the NFS-Ganesha Deployment
                      and Service are created.
                    type: boolean
                  exportPath:
                    description: ExportPath is the SaunaFS path to export (default
                      "/").
                    type: string
                  image:
                    description: |-
                      Image is the NFS-Ganesha container image to use.
                      Must include the SaunaFS FSAL (saunafs-nfs-ganesha package).
                    type: string
                  nodePort:
                    description: |-
                      NodePort is the NodePort assigned to NFS port 2049.
                      When 0 Kubernetes auto-assigns a port in the NodePort range.
                    format: int32
                    maximum: 32767
                    minimum: 30000
                    type: integer
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector for the NFS gateway pod.
                    type: object
                  replicas:
                    description: Replicas is the number of NFS gateway pods (default
                      1).
                    format: int32
                    type: integer
                  resources:
                    description: Resources for the NFS gateway container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  serviceType:
                    description: |-
                      ServiceType controls how the NFS Service is exposed.
                      Defaults to NodePort.
                    type: string
                  squash:
                    description: Squash controls NFS root squashing (default "No_Root_Squash").
                    type: string
                  tolerations:
                    description: Tolerations for the NFS gateway pod.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              shadow:
                description: |-
                  Shadow configures optional shadow master instances. Shadow masters run
                  with PERSONALITY=shadow and stay fully in sync with the active master via
                  the metadata changelog stream. The operator manages a Kubernetes Lease to
                  perform automatic leader election: if the active master pod becomes
                  unavailable the operator promotes the most up-to-date shadow to master,
                  updates the master Service selector, and demotes the recovered pod to
                  shadow — preventing split-brain.
                properties:
                  image:
                    description: Image overrides the container image (defaults to
                      the same image as Master).
                    type: string
                  metadataStorage:
                    description: |-
                      MetadataStorage configures the PVC template used by each shadow replica
                      to persist its local copy of the metadata. Each replica gets its own PVC.
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        default: 1Gi
                        description: |-
                          Size is the storage capacity requested for the metadata PVC.
                          Defaults to "1Gi".
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      storageClassName:
                        description: |-
                          StorageClassName is the name of the StorageClass to use for the metadata PVC.
                          Defaults to "" (cluster default StorageClass).
                        type: string
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector for shadow pods.
                    type: object
                  replicas:
                    default: 1
                    description: Replicas is the number of shadow instances (default
                      1).
                    format: int32
                    minimum: 1
                    type: integer
                  resources:
                    description: Resources for the shadow container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.


                          This is an alpha field and requires enabling the
                          DynamicResourceAllocation feature gate.


                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    description: Tolerations for shadow pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
            type: object
          status:
            description: SaunaFSClusterStatus defines the observed state of SaunaFSCluster.
            properties:
              activeMaster:
                description: |-
                  ActiveMaster is the name of the pod currently holding the master role
                  (either the primary master pod or a promoted shadow pod).
                  Empty if no active master is known.
                type: string
              conditions:
                description: |-
                  Conditions holds the latest observed conditions of the cluster.
                  The "Ready" condition indicates whether all components have been
                  successfully reconciled.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              readyChunkServers:
                description: |-
                  ReadyChunkServers is the number of chunk-server StatefulSets whose
                  desired replicas are all ready.
                format: int32
                type: integer
              readyMetaloggers:
                description: ReadyMetaloggers is the number of ready metalogger replicas.
                format: int32
                type: integer
              readyShadows:
                description: |-
                  ReadyShadows is the number of shadow master replicas that are Ready and
                  connected to the active master.
                format: int32
                type: integer
              totalChunkServers:
                description: |-
                  TotalChunkServers is the total number of chunk-server StatefulSets
                  configured in spec.chunk.servers.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
