---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: localdiskconfigs.disk.localdisk-operator.io
spec:
  group: disk.localdisk-operator.io
  names:
    kind: LocalDiskConfig
    listKind: LocalDiskConfigList
    plural: localdiskconfigs
    shortNames:
    - ldc
    singular: localdiskconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.agentDaemonSetName
      name: DaemonSet
      type: string
    - jsonPath: .status.agentDaemonSetReady
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LocalDiskConfig is the singleton configuration resource for the
          localdisk-operator. It controls the agent DaemonSet deployment,
          including node selection and tolerations.
          Only one LocalDiskConfig named "default" should exist in the cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LocalDiskConfigSpec defines the desired configuration for
              the localdisk-operator.
            properties:
              agent:
                description: Agent configures the disk-agent DaemonSet managed by
                  the controller.
                properties:
                  image:
                    description: |-
                      Image is the container image for the agent.
                      Defaults to the image bundled with the operator release.
                    type: string
                  includeLoopDevices:
                    description: |-
                      IncludeLoopDevices makes the agent include loop devices in discovery.
                      Useful for Kind / test environments where loop devices simulate disks.
                    type: boolean
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      NodeSelector is a label selector that restricts which nodes the agent
                      DaemonSet runs on. If empty, the agent runs on all nodes.
                      Example: {"node-role.kubernetes.io/storage": ""}
                    type: object
                  scanInterval:
                    description: |-
                      ScanInterval overrides how often the agent rescans block devices.
                      Specified as a duration string (e.g. "10s", "1m"). Defaults to 60s.
                    type: string
                  tolerations:
                    description: |-
                      Tolerations are applied to the agent DaemonSet pods.
                      By default the agent tolerates all taints so it can run on any node
                      (including control-plane). Set this to override that behaviour.
                    items:
                      description: |-
                        Toleration mirrors corev1.Toleration but is defined here to avoid
                        importing the full core/v1 package in the API types. The controller
                        converts these to corev1.Toleration when building the DaemonSet.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match.
                            Empty means match all taint effects.
                          enum:
                          - ""
                          - NoSchedule
                          - PreferNoSchedule
                          - NoExecute
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid values are Exists and Equal. Defaults to Equal.
                          enum:
                          - Exists
                          - Equal
                          type: string
                        value:
                          description: Value is the taint value the toleration matches
                            to.
                          type: string
                      type: object
                    type: array
                type: object
            type: object
          status:
            description: LocalDiskConfigStatus defines the observed state of LocalDiskConfig.
            properties:
              agentDaemonSetName:
                description: AgentDaemonSetName is the name of the managed DaemonSet.
                type: string
              agentDaemonSetReady:
                description: |-
                  AgentDaemonSetReady indicates whether the agent DaemonSet is up-to-date
                  and has the desired number of ready pods.
                type: boolean
              conditions:
                description: Conditions holds standard Kubernetes conditions.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
